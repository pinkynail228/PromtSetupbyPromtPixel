# Serena Tooling Adapter (Optional)

Adds optional Serena onboarding notes and Codex rule snippet.

## Usage

- Use bootstrap flags `--with-serena` and optionally `--with-codex-mcp`.
- This adapter is not required for baseline project readiness.

## Scope

- Documentation and optional Codex rule only.
- No mandatory hard gate in core workflow.
